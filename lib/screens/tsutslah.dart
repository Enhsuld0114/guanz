import 'package:flutter/material.dart';

class ZogsoohScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Захиалга зогсоох '),
    );
  }
}
